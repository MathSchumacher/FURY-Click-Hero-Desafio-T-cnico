PORT=3001
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
# For Upstash use a single REDIS_URL instead (rediss://...)
# REDIS_URL=
QUEUE_NAME=ad-processing
WORKER_CONCURRENCY=5

# Upstream takedown endpoint (challenge uses JSONPlaceholder).
# Override in tests or to point at a real Meta Ads sandbox.
UPSTREAM_URL=https://jsonplaceholder.typicode.com/posts/1
UPSTREAM_TIMEOUT_MS=5000
