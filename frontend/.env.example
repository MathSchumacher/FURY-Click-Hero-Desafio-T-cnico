# URL pública do backend (Render, Railway, etc.).
# Em dev local, deixe vazio — o proxy do Vite redireciona /api → http://localhost:3001.
# Em produção (Netlify), seta no painel: Site settings → Environment variables.
VITE_API_URL=
